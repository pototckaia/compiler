Type
  TA = Array[0..9,0..9] of Integer;
  TB = Array[0..9] of Integer;

var
  A,B : TA;
  c : TB;

type PL = ^integer;
	PPL = ^Pl;
	PTA = ^TA;

var  p1,p2 : ^Integer;
     pp : pointer;

     i : integer;

     ppp : PPL;
     tpta : PTA;

begin
	i := A[1][0];
	i := A[0 + i + 1, -1];
	C := A[1];
	i := ppp[10, 1];

	i := (tpta + 12)[0, 0, 0];
	i := (tpta + 12)^[0, 0];
	i := tpta[1, 0, 0];
end.